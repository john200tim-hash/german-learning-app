// src/components/lessons/StructuredTable.js

import React from 'react';

/**
 * Renders complex data (e.g., declensions) from structured JSON.
 * The data structure should conform to `GrammarSchema.ts`. [3]
 */
export default function StructuredTable({ data }) {
  return (
    <table>
      {/* Map over `data` to render table rows and cells */}
    </table>
  );
}