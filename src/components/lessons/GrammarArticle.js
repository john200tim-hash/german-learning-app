// src/components/lessons/GrammarArticle.js

import React from 'react';

/**
 * Renders rich text/markdown content fetched from a Headless CMS.
 */
export default function GrammarArticle({ content }) {
  // This component would use a library like 'react-markdown' to render content.
  return <article>{/* Render markdown content here */}</article>;
}